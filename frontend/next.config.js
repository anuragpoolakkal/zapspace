/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['img.freepik.com', 'cdn3d.iconscout.com', 'static.vecteezy.com', 'cdn-icons-png.flaticon.com', 'images.unsplash.com'],
    },
}

module.exports = nextConfig
